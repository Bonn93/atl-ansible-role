---
- name: Test Atlassian Docker role
  hosts: localhost
  become: true
  tasks:
    - name: Include the role
      include_role:
        name: your_role_name_here

    # Add additional tasks to verify the role's behavior, such as checking if Docker containers are running,
    # verifying configuration files, etc.
